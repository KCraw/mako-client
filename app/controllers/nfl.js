import Ember from 'ember';

/*

This controller needs to:
  - Tracks whether each player is excluded
  - Tracks whether each player is required

*/

export default Ember.Controller.extend({
  // matchups: Ember.computed('model', function() {
  //   return model.get('matchups').then((matchups) => {
  //     return matchups;
  //   });
  // }),
  // playerslist: Ember.computed('model', function() {
  //   return
  // });
});
