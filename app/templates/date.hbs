
<div class="container">
  <div class="row page-header">
    <div class="col-md-12">
      <h1>Contests <small>{{et-date model.date format="dddd MMMM Do, YYYY"}}</small></h1>
    </div>
  </div>
  <div class="row">
    {{#each contests as |site|}}
      <div class="col-md-6">
        <h2>{{site.name}}</h2>
        {{#each site.contests as |sport|}}
          <h3>{{sport.name}}</h3>
          <ul class="list-unstyled">
            {{#each sport.list as |contest|}}
              <li>{{#link-to contest.sport contest}}{{contest.displayName}}{{/link-to}}</li>
            {{else}}
              <li>No contests for {{et-date model.date format="MM/DD/YYYY"}}</li>
            {{/each}}
          </ul>
        {{/each}}
      </div>
    {{/each}}
  </div>
</div>
