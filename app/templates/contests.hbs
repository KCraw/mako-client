<div class="container">
  <div class="row page-header">
    <div class="col-md-12">
      <h1>Contests <small>Upcoming</small></h1>
    </div>
  </div>
  <div class="row">
    <div class="col-md-6">
      <h2>FanDuel</h2>
      <h3>MLB</h3>
      <ul class="list-unstyled">
        {{#each model.fanduelMlbContests as |contest|}}
          <li>
          {{#link-to contest.sport contest}}
            {{contest.displayName}}
          {{/link-to}}
          </li>
        {{else}}
          No upcoming contests
        {{/each}}
      </ul>
      <h3>NFL</h3>
      <ul class="list-unstyled">
        {{#each model.fanduelNflContests as |contest|}}
          <li>
          {{#link-to contest.sport contest}}
            {{contest.displayName}}
          {{/link-to}}
          </li>
        {{else}}
          No upcoming contests
        {{/each}}
      </ul>
      <h3>NBA</h3>
      <ul class="list-unstyled">
        {{#each model.fanduelNbaContests as |contest|}}
          <li>
          {{#link-to contest.sport contest}}
            {{contest.displayName}}
          {{/link-to}}
          </li>
        {{else}}
          No upcoming contests
        {{/each}}
      </ul>
    </div>
    <div class="col-md-6">
      <h2>DraftKings</h2>
      <h3>MLB</h3>
      <ul class="list-unstyled">
        {{#each model.draftkingsMlbContests as |contest|}}
          <li>
          {{#link-to contest.sport contest}}
            {{contest.displayName}}
          {{/link-to}}
          </li>
        {{else}}
          No upcoming contests
        {{/each}}
      </ul>
      <h3>NFL</h3>
      <ul class="list-unstyled">
        {{#each model.draftkingsNflContests as |contest|}}
          <li>
          {{#link-to contest.sport contest}}
            {{contest.displayName}}
          {{/link-to}}
          </li>
        {{else}}
          No upcoming contests
        {{/each}}
      </ul>
      <h3>NBA</h3>
      <ul class="list-unstyled">
        {{#each model.draftkingsNbaContests as |contest|}}
          <li>
          {{#link-to contest.sport contest}}
            {{contest.displayName}}
          {{/link-to}}
          </li>
        {{else}}
          No upcoming contests
        {{/each}}
      </ul>
    </div>
  </div>
</div>
