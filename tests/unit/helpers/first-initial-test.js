import { firstInitial } from '../../../helpers/first-initial';
import { module, test } from 'qunit';

module('Unit | Helper | first intitial');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = firstInitial(42);
  assert.ok(result);
});
