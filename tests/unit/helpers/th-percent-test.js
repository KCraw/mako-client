import { thPercent } from '../../../helpers/th-percent';
import { module, test } from 'qunit';

module('Unit | Helper | th percent');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = thPercent(42);
  assert.ok(result);
});
