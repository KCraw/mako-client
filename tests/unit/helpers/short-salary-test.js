import { shortSalary } from '../../../helpers/short-salary';
import { module, test } from 'qunit';

module('Unit | Helper | short salary');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = shortSalary(42);
  assert.ok(result);
});
