import { mathRound } from '../../../helpers/math-round';
import { module, test } from 'qunit';

module('Unit | Helper | math round');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = mathRound(42);
  assert.ok(result);
});
