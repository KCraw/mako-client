import { etDate } from '../../../helpers/et-date';
import { module, test } from 'qunit';

module('Unit | Helper | et date');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = etDate(42);
  assert.ok(result);
});
