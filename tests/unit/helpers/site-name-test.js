import { siteName } from '../../../helpers/site-name';
import { module, test } from 'qunit';

module('Unit | Helper | site name');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = siteName(42);
  assert.ok(result);
});
